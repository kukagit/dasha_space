# Аудио

Положи сюда:
- `bgm.mp3` — фоновая музыка во время игры (looped).
- `ending.mp3` — музыка для финальной открытки.

Файлы автоматически подхватятся. Если файла нет — игра просто не будет играть звук, без ошибок.

Громкость и пути настраиваются в `src/config.js` → `audio` и `ending.music`.
